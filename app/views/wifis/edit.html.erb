<%= render partial: "shared/admin_menu", locals: { title: "Wifi" } %>

<%= form_with(model: @wifi, local: true) do |form| %>
  <% if @wifi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wifi.errors.count, "error") %> prohibited this wifi from being saved:</h2>

      <ul>
      <% @wifi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class=" table left no-border ">
    <tbody>
      <tr class="field">
        <td><strong>Name: </strong></td>
        <td><%= form.text_field :name %></td>
      </tr>
      <tr class="field">
        <td><strong>Network: </strong></td>
        <td><%= form.text_field :network %></td>
      </tr>
      <tr class="field">
        <td><strong>Password: </strong></td>
        <td><%= form.text_field :password %></td>
      </tr>
    </tbody>
  </table>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<div class=" link-delete ">
  <%= link_to "Delete",  @wifi, method: :delete, data: { confirm: "Are you sure you want to delete this wifi?" } %>
</div>
